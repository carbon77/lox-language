class Point {
    init(x, y) {
        this.x = x;
        this.y = y;
    }

    sum() {
        return this.x + this.y;
    }

    combine(point) {
        return Point(this.x + point.x, this.y + point.y);
    }
}

var a = Point(1, 10);
var b = Point(4, 5);
var c = a.combine(b);
print c.sum();
